# Visualization Scripts

This folder contains the code used to generate analysis and report figures.

Final reviewer-facing images are kept separately in `../report_plots/`. Most scripts here expect local restricted MIMIC-IV-derived files or generated model outputs, so they are included for transparency rather than as the main public demo path.
